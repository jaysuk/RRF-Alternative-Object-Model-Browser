# Generated plugin ZIP — use GitHub Releases for distribution
*.zip

# src/model-data.js is generated by prebuild.js but VENDORED (committed) so a fresh clone / CI can
# build and run the mount tests without network. Regenerate with `npm run generate` (build.bat also
# regenerates it before each release build).

# Intermediate build output from `npm run build-plugin-pkg`
dist/
pkg/

# Node
node_modules/
coverage/
